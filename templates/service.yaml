apiVersion: v1
kind: Service
metadata:
  name: {{ include "auto-youtube-dl.fullname" . }}
  labels:
    {{- include "auto-youtube-dl.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "auto-youtube-dl.selectorLabels" . | nindent 4 }}
